import XCTest
@testable import MultipleFireColumn

final class MultipleFireColumnTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MultipleFireColumn().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
